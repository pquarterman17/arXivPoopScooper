{
  "name": "array without x-mergeKey: override replaces, does not union",
  "defaults": {
    "tags": ["a", "b", "c"]
  },
  "override": {
    "tags": ["x"]
  },
  "schema": {
    "type": "object",
    "properties": {
      "tags": { "type": "array", "items": { "type": "string" } }
    }
  },
  "expected": {
    "tags": ["x"]
  }
}
