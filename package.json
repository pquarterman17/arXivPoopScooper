{
  "name": "scientific-litter-scoop",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend modules for the SCQ paper database. No build step — vanilla ES modules served by serve.py.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0",
    "sql.js": "^1.10.0"
  }
}
